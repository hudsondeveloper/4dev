@model IEnumerable<USM.Models.PONTOS_PARADA_SALVADOR>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NOME_PONTO_PARADA_SALVADOR)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NOME_PONTO_PARADA_SALVADOR)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_PONTO_SALVADOR }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_PONTO_SALVADOR }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_PONTO_SALVADOR })
        </td>
    </tr>
}

</table>
